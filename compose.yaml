services:
  interaction-gateway:
    build:
      context: .
      dockerfile: apps/interaction-gateway/Dockerfile
    ports:
      - "4100:4100"
  ui-compiler-agent:
    build:
      context: .
      dockerfile: apps/ui-compiler-agent/Dockerfile
    ports:
      - "4101:4101"
