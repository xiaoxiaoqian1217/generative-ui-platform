# TASK-002：退役可运行 Fixture Provider

## 目标

退役可运行 Fixture Provider，并将确定性测试迁移到进程内测试替身。

## 交付

- 移除日常运行配置、默认环境和额外服务中的 Fixture Provider。
- 为单元、集成和浏览器测试建立进程内测试替身注入方式。
- 真实模型不可用时显示明确依赖失败，且不静默回退。
- 更新平台和开发文档。

## 验收

- 测试不需要真实模型或 Fixture 服务。
- 文档不再将可运行 Fixture 描述为默认模式。

## 依赖

无。
