# 交互过程组件：从 AG-UI 事件流到可落地的组件清单与路线

## 一、引子：每个团队都在重写工具调用卡

打开任何一个 Agent 产品的前端，你大概率会看到差不多的东西。

一段逐字出现的回复，一个可折叠的"正在调用工具"卡片，一条转圈的等待指示，偶尔冒出一个"是否允许执行"的确认按钮。

这些东西每个团队都写过，而且每个团队都在重写。

它们在组件库里没有名字。

Ant Design 有 Form 和 Table，Material UI 有 AppBar 和 Dialog，但没有任何主流组件库提供"工具调用卡""审批卡""运行状态条"。

于是设计师说"加一个工具卡"，工程师临时拼一个；下一个功能要审批卡，再拼一个。

每次都是一次性劳动，做完即弃。

这篇文章交付三样东西：

- 一张地图：一个完整 turn 里，事件如何逐点变成界面；
- 一份清单：九类组件的职责、状态与验收标准；
- 一条路线：从 MVP 到组件资产的四个步骤。

## 二、最小理论：一条事件流，两种角色

理论只讲一节，因为它是后面所有清单的推导依据。

Agent 与前端的交互，本质是两样东西：一条双向、随时间展开的事件流，外加一份随流演化的共享状态。

AG-UI 的事件类型是有限集，主要就五族。

| 事件族 | 表达什么 |
| --- | --- |
| RUN 系列 | 一次执行的生命周期 |
| TEXT_MESSAGE 系列 | 流式文本 |
| TOOL_CALL 系列 | 工具调用与结果 |
| STATE 系列 | 共享状态的快照与增量 |
| MESSAGES_SNAPSHOT | 消息历史重建 |

> 事件可枚举，所以每类事件的呈现职责可枚举，所以组件清单可盘点、可积累。

这是整篇文章的支点。

组件在这条流上只有两种角色。

| 角色 | 定义 | 例子 |
| --- | --- | --- |
| 呈现组件 | 事件流的只读投影，只消费不产生 | 流式文本、工具卡、状态徽标 |
| 动作组件 | 事件流的源，把用户意图变成新事件 | 输入框、停止按钮、审批按钮 |

一个常见的分类陷阱：按"信息方向"分，而不是按"角色"分。

审批卡的信息确实从用户流向 agent，但它的出现时机、所在位置、可选项，全部由 agent 决定。

按方向分，审批卡会和输入框混成一类，而这两样东西根本无法统一设计。

> 记住一条：角色是稳定轴，方向是易变轴。

## 三、体验地图：把一个 turn 完整走一遍

场景：用户对一个带地图的 Agent 说"把这三个告警点连成一条巡逻路线，预览给我看"。

一个完整 turn 里，事件与界面的对应关系如下。

| 步 | 流上发生什么 | 界面亮起什么 |
| --- | --- | --- |
| 1 | 用户输入并发送 | 输入框，发起时刻 |
| 2 | RUN_STARTED | 运行状态条进入"运行中" |
| 3 | TEXT_MESSAGE 系列 | 流式文本逐字渲染 |
| 4 | TOOL_CALL_START / ARGS / END | 工具调用卡直播态：工具名加流式参数 |
| 5 | 前端工具执行 previewPath | 地图画出路线，这是工具行动的本体呈现 |
| 6 | TOOL_CALL_RESULT | 工具调用卡折叠为归档态一行 |
| 7 | RUN_FINISHED，outcome 携带 interrupt | 审批卡出现：这条路线可以吗 |
| 8 | 用户确认，resume | 审批卡按下，新 run 开始 |
| 9 | TEXT_MESSAGE 系列 + Artifact | 流式文本加结果卡片 |
| 10 | RUN_FINISHED | 状态条归档，建议动作出现 |

十个步骤，覆盖了九类组件中的七类。

缺任何一步，都是一个可以直接指认的体验事故。

| 缺失 | 用户遭遇 |
| --- | --- |
| 第 4 步没有工具卡 | 盯着转圈八秒，不知道 agent 在干什么 |
| 第 7 步没有审批卡 | 被要求"打字回复是否满意" |
| 第 10 步没有建议动作 | 会话死在水面上，不知道下一步能干什么 |

这张表的正确用法：当作组件清单的验收场景。

新 Agent 接入、新版本上线，都把这个 turn 完整跑一遍，缺哪个组件当场暴露。

## 四、全景清单：九类组件一览

先给全景，第五节再挑重点展开。

### 呈现组件：五个切面

| 切面 | 回答的问题 | 代表组件 | 一句话要点 |
| --- | --- | --- | --- |
| 过程透明 | agent 正在做什么 | 运行状态条、步骤进度、思考面板、工具调用卡、流式文本 | 默认收起，点击展开 |
| 状态同步 | 现在是什么 | 状态指示器、活动流 | 呈现事实，不是流水账 |
| 结果呈现 | 最终得到什么 | 富文本回复、产物 viewer、Generative UI、摘要语义单元 | 产品差异化的主战场 |
| 异常边缘 | 出了什么事 | 错误卡、重试入口、能力缺口提示 | 缺口显式呈现，不伪造 |
| 历史组织 | 之前发生过什么 | 会话记录、轮次分组 | 归档后过程收敛为一行 |

过程与状态的分界：日志式的"调用了工具 X"是过程，"当前已选中 3 个目标"是状态。

### 动作组件：四个时刻

| 时刻 | 谁发起 | run 状态 | 代表组件 |
| --- | --- | --- | --- |
| 发起 | 用户 | 未开始 | 输入框、附件、上下文注入 |
| 介入 | 用户 | 运行中 | 暂停、停止、取消 |
| 应答 | agent | 挂起 | 审批卡、澄清选项、参数编辑 |
| 追问 | 用户 | 已结束 | 追问、建议动作、重新生成 |

### 两个共享的架构决定

这两条适用于九类组件中的每一类：

- 先物化，再渲染：事件先汇入一个集中的视图状态，组件只读状态、不各自订阅事件流，这让界面可回放、可测试；
- 每个组件都有两态：直播态（事件进行中）与归档态（进入历史），两态要分别设计，不能只设计直播态。

## 五、重点规格：四个高价值组件

九类里挑四个最值得先做对的，逐个给规格。

规格统一三件套：职责、双态、验收标准。

### 工具调用卡（复用价值最高）

- 职责：把一次工具调用的全程，压缩成可扫读的卡片。
- 直播态：工具名、状态点、参数流式追加，到多少显示多少。
- 归档态：一行摘要（工具名加结果一句话），可展开看完整参数与结果。
- 验收：流式期间界面持续有变化；结果到达后自动折叠；历史里不超过一行。

一个高频错误：把"参数传完"当成"执行完成"。

完成信号是工具结果回传，不是参数流结束。

### 运行状态条

- 职责：任何时刻回答"agent 活着吗，在干什么"。
- 直播态：不要只有转圈，带上最近活动，比如当前步骤名或最近一次事件的时间。
- 验收：网络断流时，用户十秒内能察觉"卡住了"，而不是无限转圈。

### 审批卡（应答时刻的核心）

这是全文最重要的一处观察。

输入框接受什么，由前端决定；审批卡提供哪些选项，由 agent 决定。

> 应答型组件不是前端的输入组件，而是 agent 生成出来、借前端渲染的输入组件。

这也是 Generative UI 的真正起点：agent 生成的不只是"你看什么"，还有"你能做什么"。

- 职责：把 agent 的挂起征询渲染成结构化选择。
- 直播态：问题、选项、取消出口。
- 归档态：用户的决定必须写进历史，比如"用户批准了路线 A"。
- 验收：agent 征询时，用户点按钮而不是打字。

### 建议动作（追问时刻）

- 职责：run 结束后，给用户预判好的下一步入口。
- 要点：内容随上下文变化，同样由 agent 下发。
- 验收：会话结束后界面不死掉，始终有可点的下一步。

### 三个协议事实，避免踩坑

设计前先知道三件事，能省掉很多等待：

- AG-UI 没有原生思考事件，思考面板靠自定义事件承载，呈现上默认收起；
- AG-UI 没有原生取消事件，中断就是断开连接，"已取消"是前端本地合成的状态；
- 前端工具的真正呈现常常不在聊天流里，而在界面上，比如地图画出路线，聊天流里的工具卡只是它的叙述。

## 六、沉淀规则：什么时候进公共库

两条规则，都很短。

- **双态规则**：任何过程类组件进入历史时，必须收敛为一行摘要。把会话滚回昨天检查一遍，超过一行的就是没设计归档态。
- **业务名词规则**：公共组件的接口里出现业务名词，就移出公共库。"从三个选项中选一个"是通用组件；"从三条巡逻路线中选一条"是领域组件。

通用组件尽早沉淀，领域组件等第二个真实消费者出现再抽象。

反过来做是经典陷阱：把只有一个场景的 viewer 抽成通用组件库，然后发现它的每个抽象点都是错的。

## 七、推进路线：盘点三问，四步走

### 先盘点：三问

对着最近一次真实会话的截图，问三个问题：

1. 每个界面元素能标出组件类别吗？（标不出的就是裸奔点）
2. 错误那次会话的截图长什么样？（多数产品拿不出这张图）
3. agent 征询时，用户是点按钮还是打字？（打字就是应答组件缺失）

### 再四步

| 步骤 | 做什么 | 验收 | 沉淀层 |
| --- | --- | --- | --- |
| 一：MVP 五件套 | 流式文本、状态条、工具卡、错误卡、输入与停止 | 第三节的 turn 全程无裸奔 | 协议投影 |
| 二：状态与历史 | 状态指示、活动流、历史归档、重连恢复 | 滚回昨天，过程组件全是一行 | 协议投影 |
| 三：应答型组件 | 审批卡、澄清卡，打通征询与恢复闭环 | agent 征询时用户点按钮 | 交互模式 |
| 四：语义单元 | 从结果里提炼指标、徽章、信息行词汇表 | 同一语义跨场景同形 | 语义单元 |

四步对应三层积累：协议投影人人都有，交互模式是产品判断的结晶，语义单元是领域语言的沉淀。

真正的复利发生在后两层。

MVP 五件套这一步，CopilotKit 这类框架提供默认实现，你的工作是按第五节的规格替换成自己的。

从第三步开始是框架给不了的：审批的默认值、拒绝后的走向、挂起态的展示、超时的处理，全是产品判断，全是积累。

## 八、结语

两族九类，一张地图，一条四步路线。

用法比清单更重要：

- 跑通那个 turn，你有了验收场景；
- 执行两条规则，你有了沉淀门槛；
- 走完四步，你有了资产结构。

最后一个信号值得现在记住：当输入入口本身成为 agent 的生成物，交互过程组件就不只是前端的地盘，而是 agent 与用户之间动态协商的界面协议。

组件库时代的组件是被调用出来的。

交互过程组件时代的组件，一半是渲染出来的，另一半是被生成的。

为被生成的另一半做准备，最好的起点是今天先把前一半做扎实。
